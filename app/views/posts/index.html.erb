<% @posts.each do |post| %>
	<ul>
		<li><strong>Title:</strong><%= link_to "#{post.title}", post_path(post.id) %></li>
		<li><strong>Content:</strong> <%= post.content %> </li>
		<br>
	</ul>
<% end %>
<%= link_to "Back to homepage", root_path %>
